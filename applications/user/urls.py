from django.urls import path
# from applications.user.views import RegisterAPIView  ← позже раскомментируем

urlpatterns = [
    # path('auth/register/', RegisterAPIView.as_view(), name='register'),
]

